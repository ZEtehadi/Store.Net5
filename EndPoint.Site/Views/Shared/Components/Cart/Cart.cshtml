@using Clean_Architecture.Application.Services.Carts;

@model CartDto


<div class="divider-space-card d-block">
    <div class="header-cart-basket">
        <a href="~/cart" class="cart-basket-box">
            <span class="icon-cart">
                <i class="mdi mdi-cart"></i>
            </span>
            <span class="title-cart">سبد خرید</span>

            @if (Model != null)
            {
                <span class="price-cart">

                    @Model.SumAmount.ToString("n0")

                    <span>تومان</span>
                </span>
                <span class="count-cart">@Model.ProductCount</span>
            }
        </a>
        <div class="widget-shopping-cart">
            <div class="widget-shopping-cart-content">


                @if (Model != null)
                {
                    <ul class="product-list-widget">

                        @foreach (var item in Model.CartItems)
                        {
                            <li class="mini-cart-item">
                                <div class="mini-cart-item-content">
                                    <a href="~/cart/remove?CartItemId=@item.Id" class="mini-cart-item-close">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                    <a href="#" class="mini-cart-item-image d-block">
                                        <img src="~/@item.ImageSrc">
                                    </a>
                                    <span class="product-name-card">@item.ProductName</span>
                                    <div class="variation">
                                        <span class="variation-n">فروشنده : </span>
                                        <p class="mb-0">دیجی اسمارت </p>
                                    </div>
                                    <div class="quantity">
                                        <span class="quantity-Price-amount">
                                            @item.Price.ToString("n0")
                                            <span>تومان</span>
                                        </span>
                                    </div>
                                </div>
                            </li>
                        }


                    </ul>
                    <div class="mini-card-total">
                        <strong>قیمت کل : </strong>
                        <span class="price-total">
                            @Model.SumAmount.ToString("n0")
                            <span>تومان</span>
                        </span>
                    </div>
                    <div class="mini-card-button">
                        <a href="~/cart" class="view-card">مشاهده سبد خرید</a>


                    </div>
                  @*    <div class="mini-card-button">


                         <form asp-controller="ZarinpalPay" asp-action="Payment" method="post" id="checkout-form">
                             <input type="hidden" id="amount" name="amount" value=@Model.SumAmount />

                             <div class="proceed-to-checkout">
                                 <a href="#" id="checkout-button" class="checkout-button d-block">تسویه حساب</a>
                             </div>
                         </form>

                     </div> *@

                }

            </div>
        </div>
    </div>
</div>

@* 
 @section Scripts
 {

     <script src="~/SiteTemplate/assets/js/vendor/lightgallery-all.js"></script>
     <script src="~/SiteTemplate/assets/js/vendor/jquery.ez-plus.js"></script>

     <script>
         document.getElementById('checkout-button').addEventListener('click', function (event) {
             event.preventDefault();


             document.getElementById('checkout-form').submit();
         });


     </script>

 } *@